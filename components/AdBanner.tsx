export function AdBanner({ slot }: { slot: string }) {
  // TODO: Replace with actual AdSense code after approval
  return (
    <div className="ad-banner my-6" data-slot={slot}>
      {/* AdSense code will be placed here */}
    </div>
  );
}
